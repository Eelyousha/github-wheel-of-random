# Supabase
SUPABASE_URL=
SUPABASE_KEY=

# GitHub (optional — higher rate limit for crawling)
GITHUB_TOKEN=

# Backend
BACKEND_PORT=8000
CRAWL_INTERVAL=6h

# Frontend (used at build time)
VITE_API_BASE_URL=http://localhost:8000
